{
  "scripts": {
    "build": "tsc -b && vite build",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=christmas-tree",
    "preview": "vite preview",
    "dev": "vite",
    "lint": "eslint ."
  }
}